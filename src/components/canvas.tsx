import { FunctionComponent, h } from "preact";
import { objState } from "../libs/state";

const QuickDrawCanvas: FunctionComponent = () => {
	// const RenderData = objState

	return <canvas />;
};

export default QuickDrawCanvas;
